#include "level_manager.h"
#include "level.h"

LevelManager::LevelManager()
    : _currentLevel(0) {}

LevelManager::~LevelManager() {}

Level* LevelManager::getCurrentLevel() const {
	return nullptr;
}